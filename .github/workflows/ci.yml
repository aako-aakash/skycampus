name: SkyCampus CI/CD

# Deployment is handled directly by Render (backend + AI service) and Vercel (frontend)
# dashboards via their own GitHub integration — not through this workflow.
# This workflow is disabled to avoid failures from missing secrets/lockfiles.
# Re-enable and configure secrets if you want GitHub Actions-based deployment later.

on:
  workflow_dispatch:
